import { useAuth } from "@/_core/hooks/useAuth";
import { BrandMark } from "@/components/BrandMark";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Sidebar, SidebarContent, SidebarFooter, SidebarHeader, SidebarInset, SidebarMenu, SidebarMenuButton, SidebarMenuItem, SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar";
import { useIsMobile } from "@/hooks/useMobile";
import { trpc } from "@/lib/trpc";
import { Activity, Bot, Building2, Cable, CheckCircle2, Headphones, LayoutDashboard, LibraryBig, LockKeyhole, LogOut, MailCheck, PanelsTopLeft, Workflow } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";
import { DashboardLayoutSkeleton } from "./DashboardLayoutSkeleton";

const menuItems = [
  { icon: LayoutDashboard, label: "Operations dashboard", path: "/dashboard" },
  { icon: Activity, label: "Review command centre", path: "/workspace" },
  { icon: Workflow, label: "Workflow studio", path: "/workflows" },
  { icon: Headphones, label: "Live call desk", path: "/calls" },
  { icon: Bot, label: "Agent desk", path: "/agents" },
  { icon: PanelsTopLeft, label: "Sales operations hub", path: "/sales-operations" },
  { icon: LibraryBig, label: "Approved knowledge", path: "/knowledge" },
  { icon: Building2, label: "Company setup", path: "/company-setup" },
  { icon: Cable, label: "Connections", path: "/connections" },
];

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  const { loading, user, logout } = useAuth();
  const security = trpc.security.status.useQuery(undefined, { enabled: Boolean(user) });
  if (loading || security.isLoading) return <DashboardLayoutSkeleton />;
  if (!user) return <SignedOut />;
  if (!security.data?.verified) return <SecondFactorGate hasEmail={Boolean(security.data?.hasEmail)} smtpReady={Boolean(security.data?.smtpReady)} />;
  return <SidebarProvider><Sidebar className="border-r border-white/10 bg-[#08172F] text-[#EAF1FC]"><SidebarHeader className="h-[108px] justify-center border-b border-white/10 px-5"><BrandMark/><p className="mt-3 text-[9px] font-black uppercase tracking-[.16em] text-[#809CC6]">Amarktai Network operations</p></SidebarHeader><SidebarContent className="px-3 py-6"><p className="px-3 pb-3 text-[10px] font-black uppercase tracking-[.16em] text-[#7896C1]">Operate</p><SidebarMenu>{menuItems.map(item => <AppNavItem key={item.path} {...item}/>)}</SidebarMenu><div className="mt-8 rounded-2xl border border-[#3D69AD]/30 bg-[#102A56] p-4"><div className="flex items-center gap-2 text-[#98BFFF]"><Activity size={16}/><span className="text-[10px] font-black uppercase tracking-[.13em]">Control state</span></div><p className="mt-2 text-xs leading-5 text-[#B1C7E7]">Work is prepared, reviewed, and recorded in the workspace before an approved external action is attempted.</p></div></SidebarContent><SidebarFooter className="border-t border-white/10 p-3"><DropdownMenu><DropdownMenuTrigger asChild><button className="flex w-full items-center gap-3 rounded-xl p-2 text-left transition hover:bg-white/[.06]"><Avatar className="size-9 border border-white/15 bg-[#153B7A]"><AvatarFallback className="bg-[#153B7A] text-xs font-bold text-[#BBD2FF]">{user.name?.slice(0, 1).toUpperCase() ?? "A"}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-[#EDF4FF]">{user.name || "Amarktai user"}</p><p className="truncate text-xs text-[#8EA8D0]">{user.email || "Authenticated workspace"}</p></div></button></DropdownMenuTrigger><DropdownMenuContent align="end" className="w-52"><DropdownMenuItem onClick={logout} className="cursor-pointer text-destructive focus:text-destructive"><LogOut className="mr-2 size-4"/>Sign out</DropdownMenuItem></DropdownMenuContent></DropdownMenu></SidebarFooter></Sidebar><SidebarInset className="bg-[#071326]"><AppTopbar/><main className="min-h-[calc(100vh-70px)] p-4 sm:p-6 lg:p-8">{children}</main></SidebarInset></SidebarProvider>;
}

function SignedOut() { return <div className="grid min-h-screen place-items-center bg-[#071326] p-5 text-[#F5F7FB]"><div className="w-full max-w-md rounded-[2rem] border border-white/10 bg-[#0C1E3E] p-8 shadow-[0_28px_70px_rgba(0,0,0,.4)]"><BrandMark/><div className="mb-5 mt-11 grid size-12 place-items-center rounded-2xl bg-[#153B7A] text-[#A9C7FF]"><Bot size={23}/></div><p className="mb-2 text-xs font-black uppercase tracking-[.16em] text-[#8CB7FF]">Protected operations</p><h1 className="font-display text-4xl font-bold tracking-[-.065em]">A clear view of the sales day.</h1><p className="mt-4 leading-6 text-[#A9BFDF]">Sign in to open the operations dashboard, prepare governed work, review decisions, and manage the team’s working context.</p><Button onClick={() => window.location.assign("/auth")} className="mt-8 h-12 w-full rounded-xl bg-[#1B64F2] font-bold text-white hover:bg-[#2B76FF]">Open secure access</Button></div></div>; }

function SecondFactorGate({ hasEmail, smtpReady }: { hasEmail: boolean; smtpReady: boolean }) { const [requested, setRequested] = useState(false); const [code, setCode] = useState(""); const status = trpc.security.status.useQuery(); const requestCode = trpc.security.requestEmailCode.useMutation({ onSuccess: () => { setRequested(true); toast.success("A verification code was sent to your authenticated email."); }, onError: error => toast.error(error.message) }); const verifyCode = trpc.security.verifyEmailCode.useMutation({ onSuccess: () => { toast.success("Access verification complete."); status.refetch(); window.location.assign("/dashboard"); }, onError: error => toast.error(error.message) }); return <div className="grid min-h-screen place-items-center bg-[#071326] p-5 text-[#F5F7FB]"><div className="grid w-full max-w-4xl overflow-hidden rounded-[2rem] border border-white/10 bg-[#0C1E3E] shadow-[0_28px_80px_rgba(0,0,0,.42)] lg:grid-cols-[.9fr_1.1fr]"><section className="bg-[#0A1830] p-8 sm:p-10"><BrandMark/><div className="mt-16 grid size-12 place-items-center rounded-2xl bg-[#153B7A] text-[#A9C7FF]"><LockKeyhole size={21}/></div><p className="mt-7 text-xs font-black uppercase tracking-[.15em] text-[#8CB7FF]">Access verification</p><h1 className="mt-3 font-display text-5xl font-bold leading-[.88] tracking-[-.075em]">Protect the work that matters.</h1><p className="mt-5 text-sm leading-6 text-[#A9BFDF]">Verify your email before the operations dashboard can show workload, review decisions, call context, connections, and audit activity.</p><div className="mt-8 space-y-3 text-sm text-[#C4D5F0]"><p className="flex gap-2"><CheckCircle2 className="mt-0.5 size-4 shrink-0 text-[#8CB7FF]"/>Codes expire after ten minutes.</p><p className="flex gap-2"><CheckCircle2 className="mt-0.5 size-4 shrink-0 text-[#8CB7FF]"/>Verification persists for twelve hours.</p></div></section><section className="p-8 sm:p-10"><div className="grid size-11 place-items-center rounded-2xl bg-[#153B7A] text-[#A9C7FF]"><MailCheck size={20}/></div><h2 className="mt-6 font-display text-4xl font-bold leading-[.9] tracking-[-.07em]">Confirm access.</h2>{!hasEmail ? <Notice text="This account has no configured email address. Add the administrator email in the self-hosted configuration before enabling access verification."/> : !smtpReady ? <Notice text="Email verification is implemented but SMTP values are not configured yet. Add the SMTP settings in the Webdock environment before sending access codes."/> : !requested ? <><p className="mt-4 text-sm leading-6 text-[#A9BFDF]">We will send a six-digit code to the authenticated email address.</p><Button onClick={() => requestCode.mutate()} disabled={requestCode.isPending} className="mt-8 h-12 w-full rounded-xl bg-[#1B64F2] font-bold hover:bg-[#2B76FF]">{requestCode.isPending ? "Sending code…" : "Send verification code"}</Button></> : <><p className="mt-4 text-sm leading-6 text-[#A9BFDF]">Enter the code sent to your authenticated email.</p><input inputMode="numeric" maxLength={6} value={code} onChange={event => setCode(event.target.value.replace(/\D/g, ""))} placeholder="000000" className="mt-6 h-14 w-full rounded-xl border-2 border-white/15 bg-[#08172F] text-center font-display text-3xl font-bold tracking-[.32em] text-white outline-none transition placeholder:text-[#56749E] focus:border-[#4E8BFF]"/><Button onClick={() => verifyCode.mutate({ code })} disabled={code.length !== 6 || verifyCode.isPending} className="mt-4 h-12 w-full rounded-xl bg-[#1B64F2] font-bold hover:bg-[#2B76FF]">{verifyCode.isPending ? "Verifying…" : "Verify and open dashboard"}</Button><button onClick={() => requestCode.mutate()} className="mt-4 w-full text-sm font-bold text-[#8CB7FF]">Send a new code</button></>}</section></div></div>; }

function Notice({ text }: { text: string }) { return <p className="mt-5 rounded-xl border border-[#3866AA]/35 bg-[#102A56] p-4 text-sm leading-6 text-[#BDD2F4]">{text}</p>; }
function AppNavItem({ icon: Icon, label, path }: (typeof menuItems)[number]) { const [location, setLocation] = useLocation(); const active = location === path; return <SidebarMenuItem><SidebarMenuButton isActive={active} onClick={() => setLocation(path)} tooltip={label} className={`h-11 rounded-xl px-3 transition-all hover:bg-white/[.07] hover:text-white ${active ? "bg-[#153B7A] text-white hover:bg-[#153B7A]" : "text-[#A4B9D9]"}`}><Icon className="size-[18px]"/><span className="font-semibold">{label}</span></SidebarMenuButton></SidebarMenuItem>; }
function AppTopbar() { const isMobile = useIsMobile(); if (!isMobile) return <header className="flex h-[70px] items-center justify-between border-b border-white/10 bg-[#08172F]/75 px-8 backdrop-blur"><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#8CA9D4]">Amarktai Network / Operations</p><div className="flex items-center gap-2 rounded-full border border-[#3D69AD]/35 bg-[#102A56] px-3 py-1.5 text-xs font-bold text-[#B7CFFF]"><span className="size-1.5 rounded-full bg-[#5C92FF]"/>System interface ready</div></header>; return <header className="flex h-14 items-center border-b border-white/10 bg-[#08172F] px-2"><SidebarTrigger className="rounded-lg text-white hover:bg-white/10"/><span className="ml-2 text-sm font-bold text-white">Amarktai Network</span></header>; }
